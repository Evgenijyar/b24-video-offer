services:
  app:
    build: .
    container_name: b24-video-offer
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - video_offer_data:/app/data

volumes:
  video_offer_data:
